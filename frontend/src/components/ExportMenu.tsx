export function ExportMenu() {
  return (
    <section style={{ border: "1px solid #ddd", borderRadius: 8, padding: 12, background: "#fff" }}>
      <h2 style={{ marginTop: 0 }}>Export</h2>
      <p>CSV, Excel, and JSON exports are exposed through the proxy API.</p>
    </section>
  );
}
